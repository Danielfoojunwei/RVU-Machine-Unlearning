# RVU-Machine-Unlearning

Implementation lives in [`rvu/`](rvu).

The project includes:
- RVU core (provenance closure, unlearning, certificates, auditor),
- empirical benchmark adapters for AgentDojo, InjecAgent, and BIPIA,
- GPU-ready Docker + benchmark orchestration scripts.
